mutation GdprDeleteMyProfileMutation(
  $input: DeleteMyProfileMutationInput!
  $language: TranslationLanguage!
) {
  deleteMyProfile(input: $input) {
    results {
      success
      errors {
        code
      }
      service {
        name
        title(language: $language)
      }
    }
  }
}
