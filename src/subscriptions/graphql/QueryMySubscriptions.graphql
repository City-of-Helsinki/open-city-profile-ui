query QueryMySubscriptions {
  myProfile {
    id
    subscriptions {
      edges {
        node {
          id
          enabled
          subscriptionType {
            code
          }
        }
      }
    }
  }
}
